import React from 'react'
import { Text } from 'react-native'
import Filho from './Filho'

export default ({}) => {
    let x = 100
    let y = 200

    return (
        <Filho a={x} b={y}></Filho>
    )
}